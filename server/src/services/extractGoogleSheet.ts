export default async function extractGoogleSheet(
  url: string,
  category: "class-routine" | "exam-routine"
): Promise<{
  title: string;
  content: string;
}> {
  const title = "";
  const content = "";

  

  return {
    title,
    content,
  };
}
